#ifndef TESTS_H
#define TESTS_H

#include <gtest/gtest.h>

#include <cstdlib>
#include <map>
#include <set>

#include "../lib/st_map.h"
#include "../lib/st_multiset.h"
#include "../lib/st_set.h"

auto v1 = rand() % 100;
auto v2 = rand() % 100;
auto v3 = rand() % 100;
auto v4 = rand() % 100;
auto v5 = rand() % 100;

#endif  // TESTS_H